pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}
include ':app'

def tauriSettings = file('tauri.settings.gradle')
if (tauriSettings.exists()) {
    apply from: tauriSettings
}
